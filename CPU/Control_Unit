******************************************************************** We will make a a new folder named OS 
************** and it will be a central path manager file so every script can find everything
************** no matter what machine or folder it’s in.



import subprocess   # Lets us run another Python file (Tokenization.py) as a separate process        (LEARN HOW TO USE THIS)
import json         # Used to convert JSON text into Python data (dict)                              (LEARN HOW TO USE THIS)
import os           # Used for file paths (cross-platform safe)                                      (LEARN HOW TO USE THIS)

token = 0           # Control flag (used to decide when to run this block)


def control_unit(answer):

    if token == 0:


        spacy_python = r"C:\Users\andre\OneDrive\Desktop\Portfolio\.venv-spacy\Scripts\python.exe"
        script_path = r"C:\Users\andre\OneDrive\Desktop\Portfolio\J.A.R.V.I.S\Lexical_Analysis\Tokenization.py"

        result = subprocess.run(
            [spacy_python, script_path, answer],
            capture_output=True,
            text=True
        )

        print("STDOUT:")
        print(result.stdout)

        print("STDERR:")
        print(result.stderr)

        print("Return code:", result.returncode)

        if result.returncode == 0 and result.stdout.strip():
            data = json.loads(result.stdout)

            print("Sentences:", data["sentences"])
            print("Words:", data["words"])
        else:
            print("The subprocess did not return valid JSON output.")



